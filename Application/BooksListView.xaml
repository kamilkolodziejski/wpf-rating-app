<UserControl x:Class="Kolodziejski.RatingApp.Application.BooksListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Border DockPanel.Dock="Top" BorderBrush="Black" BorderThickness="0,0,1,0">
            <Button Content="Dodaj książkę" Command="{Binding DataContext.DisplayBookView, RelativeSource={RelativeSource AncestorType={x:Type ApplicationWindow}, Mode=OneWay}}"/>
        </Border>
        <ListBox >
            
        </ListBox>
    </DockPanel>
</UserControl>
